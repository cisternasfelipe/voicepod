<Window x:Class="VoiceFlow.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:VoiceFlow.App.ViewModels"
        xmlns:res="clr-namespace:VoiceFlow.App.Resources"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="VoicePod"
        Icon="pack://application:,,,/VoiceFlow;component/Resources/voiceflow.ico"
        Height="520"
        Width="720"
        MinHeight="420"
        MinWidth="560"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock Text="VoicePod" Style="{StaticResource HeaderText}" />
                <TextBlock Text="{x:Static res:Strings.MainSubtitle}" Style="{StaticResource MutedText}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="{x:Static res:Strings.TrayHistory}"
                        Margin="0,0,8,0"
                        Padding="12,6"
                        Command="{Binding OpenHistoryCommand}" />
                <Button Content="{x:Static res:Strings.TraySettings}"
                        Padding="12,6"
                        Command="{Binding OpenSettingsCommand}" />
            </StackPanel>
        </Grid>

        <!-- Hotkey error banner -->
        <Border Grid.Row="1"
                Background="#33E05A5A"
                BorderBrush="{StaticResource DangerBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,8"
                Margin="0,0,0,12"
                Visibility="{Binding HotkeyError, Converter={StaticResource NullToCollapsedConverter}}">
            <TextBlock Text="{Binding HotkeyError}" Style="{StaticResource BodyText}" />
        </Border>

        <!-- Status card -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding StatusText}" Style="{StaticResource SectionText}" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding StateDetail}" Style="{StaticResource MutedText}" />

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="{x:Static res:Strings.MainHotkeyLabel}" Style="{StaticResource MutedText}" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding HotkeyText}" Style="{StaticResource BodyText}" FontWeight="SemiBold" />
                        <TextBlock Text="  ·  " Style="{StaticResource MutedText}" />
                        <TextBlock Text="{Binding ModeText}" Style="{StaticResource MutedText}" />
                        <TextBlock Text="  ·  " Style="{StaticResource MutedText}" />
                        <TextBlock Text="{Binding ModelStatusText}" Style="{StaticResource MutedText}" />
                    </StackPanel>

                    <!-- Level meter -->
                    <Border Height="10"
                            Margin="0,14,0,0"
                            Background="{StaticResource SurfaceAltBrush}"
                            CornerRadius="5"
                            HorizontalAlignment="Stretch">
                        <ProgressBar Minimum="0"
                                     Maximum="1"
                                     Value="{Binding AudioLevel, Mode=OneWay}"
                                     Height="10"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{StaticResource SuccessBrush}" />
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                    <Button Content="{x:Static res:Strings.MainDictate}"
                            Style="{StaticResource AccentButton}"
                            Width="120"
                            Command="{Binding ToggleDictationCommand}" />
                    <Button Content="{x:Static res:Strings.Cancel}"
                            Width="120"
                            Margin="0,8,0,0"
                            IsEnabled="{Binding IsRecording}"
                            Command="{Binding CancelDictationCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Result -->
        <Border Grid.Row="3" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{x:Static res:Strings.MainLastDictation}" Style="{StaticResource SectionText}" />
                <TextBox Grid.Row="1"
                         Text="{Binding LastResult, Mode=OneWay}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" />
            </Grid>
        </Border>

        <TextBlock Grid.Row="4"
                   Text="{Binding LastRecordingInfo}"
                   Style="{StaticResource MutedText}"
                   Margin="0,12,0,0"
                   TextTrimming="CharacterEllipsis" />
    </Grid>
</Window>
