<Window x:Class="VoiceFlow.App.Views.ModelSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:VoiceFlow.App.ViewModels"
        xmlns:res="clr-namespace:VoiceFlow.App.Resources"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ModelSetupViewModel}"
        Title="{x:Static res:Strings.SetupTitle}"
        Height="380"
        Width="620"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="{x:Static res:Strings.SetupHeader}" Style="{StaticResource HeaderText}" />
            <TextBlock Text="{x:Static res:Strings.SetupSubtitle}"
                       Style="{StaticResource MutedText}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{x:Static res:Strings.SetupTargetFolder}" Style="{StaticResource SectionText}" />
                <TextBox Text="{Binding TargetDirectory, Mode=OneWay}" IsReadOnly="True" />
                <TextBlock Text="{Binding SourceUrl}"
                           Style="{StaticResource MutedText}"
                           Margin="0,8,0,0"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2">
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource BodyText}" Margin="0,0,0,10" />

            <ProgressBar Height="10"
                         Minimum="0"
                         Maximum="100"
                         Value="{Binding Progress, Mode=OneWay}"
                         Background="{StaticResource SurfaceAltBrush}"
                         BorderThickness="0"
                         Foreground="{StaticResource AccentBrush}"
                         Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisibilityConverter}}" />

            <TextBlock Text="{Binding ProgressText}"
                       Style="{StaticResource MutedText}"
                       Margin="0,6,0,0"
                       Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisibilityConverter}}" />

            <Border Background="#33E05A5A"
                    BorderBrush="{StaticResource DangerBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="12,8"
                    Margin="0,12,0,0"
                    Visibility="{Binding ErrorText, Converter={StaticResource NullToCollapsedConverter}}">
                <TextBlock Text="{Binding ErrorText}" Style="{StaticResource BodyText}" />
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static res:Strings.SetupUseLocalFolder}"
                    Command="{Binding UseLocalFolderCommand}"
                    IsEnabled="{Binding IsDownloading, Converter={StaticResource InverseBooleanConverter}}"
                    Margin="0,0,8,0" />
            <Button Content="{x:Static res:Strings.Cancel}"
                    Command="{Binding CancelCommand}"
                    IsEnabled="{Binding IsDownloading}"
                    Margin="0,0,8,0" />
            <Button Content="{x:Static res:Strings.SetupDownload}"
                    Style="{StaticResource AccentButton}"
                    Command="{Binding DownloadCommand}"
                    IsEnabled="{Binding IsDownloading, Converter={StaticResource InverseBooleanConverter}}" />
        </StackPanel>
    </Grid>
</Window>
